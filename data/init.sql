CREATE DATABASE degrees;

use degrees;

CREATE TABLE degree (
    degreeid varchar(255),
	letter varchar(255),
    bp varchar(255),
	mp varchar(255),
	dp varchar(255),
	link varchar(255),
    degreename varchar(255),
    school varchar(255)
);
